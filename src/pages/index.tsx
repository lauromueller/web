import React, { FunctionComponent } from 'react';
import { BasicPage } from '../layout';

const Index: FunctionComponent = () => {
  return <BasicPage>Once again empty</BasicPage>;
};

export default Index;
